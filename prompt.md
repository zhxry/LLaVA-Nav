**无人机遥感图路径规划问题**

现在你要根据给出的遥感图像实现一个无人机路径规划问题。路径规划需要考虑以下几点：
- 图像的分辨率为 `512px * 512px`，左上角为图像坐标 `(0, 0)`。
- 图像中红色圆为起点，黄色圆为终点，实心圆半径为 `15px`。
- 蓝色圆（或矩形）表示必经点（或区域），必须有至少一个航点经过该圆或矩形区域。
- 绿色点（或矩形）表示禁飞点（或区域），航线必须与该点或区域保持一定距离，避免无人机进入禁飞区域。
- 这是视觉语言导航，假设无人机无法接收外界定位信号、仅依靠视觉信息和预设导航指令完成整个导航任务。因此，你需要读取图像中的地标信息，选择视觉特征明显、面积适中的航点。
- 路径规划的首要原则是整体距离尽量短；同时由于无人机没有外界信号、无法依赖里程计等传感器，因此相邻航点的距离不宜过长以防止产生角度和距离误差。
- 最后请**直接给出**一个包含起点和终点在内的航点坐标序列，格式如下：
```text
[(300, 250), (227, 287), (160, 275), (120, 223), (90, 150)]
```